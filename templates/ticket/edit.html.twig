{% extends 'base.html.twig' %}

{% block title %}Edit Ticket{% endblock %}

{% block body %}
    <h1>Edit Ticket</h1>

    {{ include('ticket/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="d-flex ">
        <button onclick="location.href='{{ path('app_dashboard') }}'" class="btn btn-secondary">Back to list</button>
        {{ include('ticket/_delete_form.html.twig') }}
    </div>
{% endblock %}
