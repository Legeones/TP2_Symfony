{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Edit User</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="d-flex ">
        <button onclick="location.href='{{ path('app_user_index') }}'" class="btn btn-secondary">Back to list</button>
        {{ include('user/_delete_form.html.twig') }}
    </div>

{% endblock %}
