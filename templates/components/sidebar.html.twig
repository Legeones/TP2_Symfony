<!-- templates/components/sidebar.html.twig -->
<div class="d-flex flex-column flex-shrink-0 bg-light w-100 p-2" style="height: calc(100vh - 120px)" >
    <ul class="nav nav-pills flex-column mb-auto">
        <li class="nav-item">
            <a href="{{ path('app_dashboard') }}" class="nav-link active" aria-current="page">
                Dashboard
            </a>
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('app_user_index') }}" class="nav-link">
                    Gestion utilisateurs
                </a>
            {% endif %}
        </li>
    </ul>
    <hr>
    <div>
        <a href="#" class="d-flex align-items-center text-decoration-none">
            <strong>User</strong>
        </a>
    </div>
</div>
